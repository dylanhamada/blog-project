import React from 'react';

const blogInput = props => {
    return (
        <div>
            <textarea placeholder="Blog post text"></textarea>
        </div>
    );
}

export default blogInput;